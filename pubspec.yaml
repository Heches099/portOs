name: auto_port
description: Modern port operations dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  intl: ^0.19.0
  provider: ^6.1.2
  video_player: ^2.9.2
  firebase_auth: ^5.1.1
  firebase_core: ^3.3.3
  camera: ^0.12.0+1
  battery_plus: ^6.2.1
  connectivity_plus: ^6.1.3
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  cloud_firestore: ^5.6.12
  firebase_ai: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
